# Python
backend/.venv/
backend/.env
__pycache__/
*.pyc

# Flutter (el proyecto trae su propio .gitignore en app/)
build/
.dart_tool/

# Editores
.idea/
.vscode/
*.iml

# Capturas reales de campo extraidas de un telefono: audio de conversaciones,
# fotos de fincas, GPS y la base sqlite del dispositivo. Datos de personas, no
# fixtures: nunca al repositorio (que ademas es publico).
extraidos/

# Firma de Android. Quien tenga esto puede publicar una actualizacion que los
# telefonos aceptan como legitima: no entra al repositorio nunca.
app/android/key.properties
*.jks
*.keystore
